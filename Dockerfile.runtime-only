# Runtime-only Dockerfile that assumes WAR is built locally
FROM tomcat:8.5-jdk8-openjdk-slim

# Copy pre-built WAR file
COPY orr-ont/target/scala-2.11/orr-ont_2.11-*.war /usr/local/tomcat/webapps/ont.war

EXPOSE 8080
